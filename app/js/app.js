'use strict';

// Declare app level module which depends on filters, and services
angular.module("tycoons-app", [])
   .controller('helloWorldCtrl', function($scope){
		$scope.name = "Hello World!";	
});